# optional — only write tools (issues, comments, labels) need credentials.
# all reads go through bobbin (api.tangled.org) with no auth.
TANGLED_HANDLE=your.handle
TANGLED_PASSWORD=your-app-password
# optional: only if PDS auto-discovery via plc.directory doesn't work
TANGLED_PDS_URL=
